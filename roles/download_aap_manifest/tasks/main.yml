---
- name: Download AAP manifest
  ansible.builtin.get_url:
    url: "{{ download_aap_manifest_url }}"
    dest: "{{ download_aap_manifest_dest }}"
    mode: "0600"
  delegate_to: localhost
  become: false
